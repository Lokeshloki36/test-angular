# This project uses pnpm. Do NOT run `npm install` or `yarn install`.
# Use: pnpm install
#
# The pnpm virtual store (node_modules/.pnpm) uses symlinks that are
# incompatible with npm's Arborist resolver and will cause crashes.

# Prevent npm from being used accidentally in CI environments
engine-strict=true
